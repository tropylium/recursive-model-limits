# Dataset name for loading via dataset_factory
name: CIFAR100

# Dataset properties
num_classes: 100
input_channels: 3
image_size: 32

# Override model num_classes based on dataset
arch:
  config:
    num_classes: 100

